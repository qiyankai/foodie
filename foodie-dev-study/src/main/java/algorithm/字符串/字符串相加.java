package algorithm.字符串;


public class 字符串相加 {
    public String addStrings(String num1, String num2) {
        StringBuilder res = new StringBuilder();
        int i = num1.length() < num2.length() ? num1.length() : num2.length();
        while (--i >= 0) {
            
        }
        return res.toString();
    }
}
